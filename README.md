# graphql-doc-gen

CLI to generate Markdown/HTML/PDF docs from GraphQL introspection JSON.

## Install (local)
```bash
git clone https://github.com/Aadhiesec1/graphql-doc-gen
cd graphql-doc-gen
pip install .

## Project still in development - PDF Works Undergoing!!!
